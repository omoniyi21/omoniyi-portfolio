// Shared pastel palette for case-study cards — the same accent range used in
// the homepage's featured-work stack (see components/work/SelectedWork.jsx),
// reused here so the case-studies list and each case study's own page read
// as one system instead of two different visual languages.
export const cardTints = ["#e7edfb", "#e9e3fa", "#f6e8f5", "#f0e1f7", "#fbe6ee"];
